import VDialog from '@/components/VDialog.vue';
import VTable from '@/components/VTable.vue';

export default function baseComponents(app) {
  app.component('VDialog', VDialog);
  app.component('VTable', VTable);
}
